import { makeStyles } from "@material-ui/core/styles";


const useStyle = makeStyles(() => ({
    root: {
      marginTop: "6vh",
      color: "#c0c0c0",
    },
    card: {
      minWidth: 275,
    },
    editingLayout: {
      marginTop: "10px",
    },
  }));

  export default useStyle;